# Tutorial pages (generated)

Markdown files in this directory are **not** edited by hand.

They are produced from Literate sources in `docs/literate/tutorials/` when you run:

```bash
julia --project=docs docs/make.jl
```

Generated `*.md` files are gitignored. Only the `.jl` sources are tracked in git.
